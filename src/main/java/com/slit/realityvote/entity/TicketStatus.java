package com.slit.realityvote.entity;

/**
 * "Each complaint should automatically create a support ticket with a
 * unique reference number. The ticket should be assigned a status such
 * as Open, In Progress, Resolved, or Closed." (requirements doc)
 */
public enum TicketStatus {
    OPEN,
    IN_PROGRESS,
    RESOLVED,
    CLOSED
}
